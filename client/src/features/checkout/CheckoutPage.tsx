import { Typography } from "@mui/material";

export default function CheckoutPage() {
  return (
    <Typography>
        Only authorised users should be able to see this page. You can test this by logging out and trying to access the page again.
    </Typography>
  )
}